// DataTable: Will provide a reusable data table with sorting, filtering, pagination,
// row selection, and bulk actions. Built on top of TanStack Table.

"use client";

import React from "react";

export default function DataTable() {
  return (
    <div className="rounded-lg border">
      <div className="p-4">
        <p className="text-sm text-muted-foreground">
          Data table placeholder — coming soon
        </p>
      </div>
    </div>
  );
}
