// BulkUploadPreview: Will display a preview table of parsed Excel data before
// confirming the bulk product import, with editable cells for corrections.

"use client";

import React from "react";

export default function BulkUploadPreview() {
  return (
    <div className="rounded-lg border p-4">
      <h3 className="font-semibold">Upload Preview</h3>
      <p className="mt-2 text-sm text-muted-foreground">
        Bulk upload preview placeholder — coming soon
      </p>
    </div>
  );
}
