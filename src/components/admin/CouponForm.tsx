// CouponForm: Will provide a form for creating/editing discount coupons with
// code, discount type, value, min order, expiry, and usage limit fields.

"use client";

import React from "react";

export default function CouponForm() {
  return (
    <div className="rounded-lg border p-6">
      <h3 className="text-lg font-semibold">Coupon Form</h3>
      <p className="mt-2 text-sm text-muted-foreground">
        Coupon form placeholder — coming soon
      </p>
    </div>
  );
}
