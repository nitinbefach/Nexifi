// PaymentMethodSelector: Will display payment method options (Razorpay, COD, UPI)
// with selection state, COD availability check, and payment method icons.

"use client";

import React from "react";

export default function PaymentMethodSelector() {
  return (
    <div className="space-y-2 rounded-lg border p-4">
      <h3 className="font-semibold">Payment Method</h3>
      <p className="text-sm text-muted-foreground">
        Payment method selector placeholder — coming soon
      </p>
    </div>
  );
}
